<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-green-100 flex items center justify-center min-h-screen">
   <div class="bg-green-200 p-8 rounded-2xl shadow-lg w-full max-w-md h-[50%]">
       <h2 class="text-2xl font-bold mb-6 text-center text-green-800">Login</h2>

       <form method="POST" class="space-y-4" action="/login">
    <%if (success.length > 0) { %>
      <div style="color: green;"> <%= success %></div>
    <% } %>

    <%if (error.length > 0) { %>
      <div style="color: red;"> <%= error %></div>
    <% } %>
   <div>
       <label class="block text-sm font-medium text-green-700 mb-1">Username</label>
       <input name="username" required
       class="w-full px-4 py-2 border border-green-500 rounded-lg focus:outline-green-500 focus:ring-2 focus:ring-green-500"><br>
  </div> 
  <div>
     <label class="block text-sm font-medium text-green-700 mb-1">Password</label>
     <input name="password" type="password" required
     class="w-full px-4 py-2 border border-green-500 rounded-lg focus:outline-green-500 focus:ring-2 focus:ring-green-500"> <br>
  </div>

  <button type="submit" class="w-[50%] bg-green-500 text-white py-2 px-4 rounded-lg hover:bg-green-600">Login</button>
  <p>Dont you have Account ?</p>
  <a href="/add" class="text-green-800 hover:underline">Sign In</a>
 </form>
</div>
</body>
</html>