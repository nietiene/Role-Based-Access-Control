<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Account</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-green-50 min-h-screen flex items-center justify-center">
    <div class="bg-white p-8 rounded-2xl shadow-md w-full max-w-lg">
    <h1 class="text-2xl font-bold text-green-700 text-center mb-4">User Dashboard</h1>
    <p class="text-center text-green-700 mb-4">Welcome <%= user.username %></p>
    <div class="flex justify-end mb-4">
         <a href="/logout" class="text-sm text-white bg-red-500 hover:bg-red-600 px-4 py-2 transition duration-200 rounded-lg">Logout</a>
    </div>
    <h3 class="text-2xl font-semibold text-green-700 mb-4">Your Info:</h3>
    <ul class="text-green-700 space-y-4 mb-6">
        <li><span class="font-medium">ID:</span> <%= user.id %></li>
        <li><span class="font-medium">Name:</span> <%= user.username %></li>
        <li><span class="font-medium">password:</span> <%= user.role %></li>
    </ul>
 <div class="flex justify-between">
    <a href="/edit/<%= user.id %>"
        class="bg-green-600 hover:bg-green-700 text-white px-4 py-2 rounded-lg transition duration-200">Edit Your Info</a>
     <form action="/delete/<%= user.id %>" method="post">
        <button onclick="return confirm('Are you sure you want to delete your account?');"
        class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-lg transition duration-200">
            Delete
        </button>
     </form>
    </div>
 </div>
</body>
</html>